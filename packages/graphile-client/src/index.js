export class GraphileClient {
  constructor (meta) {
    this.meta = meta;
    this.query = '';
  }

  model(model) {
    this.model = model;
    return this;
  }
  
  getMany() {
    this.query = 
    return this;
  }
}
