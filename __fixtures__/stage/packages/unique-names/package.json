{
  "name": "@launchql/unique-names",
  "version": "0.0.1",
  "author": "Dan Lynch <pyramation@gmail.com>",
  "description": "undefined",
  "main": "index.js",
  "module": "esm/index.js",
  "types": "index.d.ts",
  "homepage": "https://github.com/launchql/pg",
  "license": "MIT",
  "publishConfig": {
    "access": "restricted",
    "directory": "dist"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/launchql/pg"
  },
  "bugs": {
    "url": "https://github.com/launchql/pg/issues"
  },
  "scripts": {
    "clean": "makage clean",
    "prepare": "npm run build",
    "build": "makage clean && makage build-ts && makage assets",
    "build:dev": "npm run clean; tsc -p tsconfig.json --declarationMap; tsc -p tsconfig.esm.json; npm run copy",
    "lint": "eslint . --fix",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "keywords": [],
  "devDependencies": {
    "graphile-test": "^2.3.0",
    "graphql-tag": "2.12.6",
    "pgsql-test": "^2.6.0"
  }
}